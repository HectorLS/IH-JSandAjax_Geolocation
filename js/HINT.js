Tinder = window.Tinder || {};
Tinder.functionName = function() {
  // do your thing here
}
Tinder.functionWithCallback = function(callback) {
  // do your thing here
  // when done, holla back
  callback();
}